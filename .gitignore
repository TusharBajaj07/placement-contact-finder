# --- Secrets: NEVER commit ---
.env
*.key
key
**/key

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# --- OS / editor ---
.DS_Store
.claude/settings.local.json

# --- Local databases / large binaries (optional: remove if you want to ship samples) ---
*.db

# --- Kept locally, NOT published (separate / unrelated tools) ---
archive/data-mining/
archive/resume-verify/
